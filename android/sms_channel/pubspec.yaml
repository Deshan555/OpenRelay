name: sms_channel
description: Local SMS Sender plugin for OpenRelay.
version: 0.0.1
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.openrelay.sms_channel
        pluginClass: SmsChannelPlugin
